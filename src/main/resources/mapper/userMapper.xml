<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
  
  <mapper namespace="com.green.users.mapper.UserMapper" >
  
  <select id="getUserList" resultType="com.green.users.dto.UserDTO">
	SELECT 
	    USERID,
	    PASSWORD,
	    USERNAME,
	    EMAIL,
	    UPOINT,
	    REGDATE
	FROM TUSER  
  </select>
  
  <select id="getIdDupCheck">
 	SELECT 	USERID,
			PASSWORD,
  			USERNAME,
			EMAIL,
			UPOINT,
			TO_CHAR(REGDATE, 'YYYY-MM-DD HH24:MI:SS') REGDATE 
	FROM	tuser
	WHERE	userid=#{userid}
  </select>
  
  <select id="getUser">
  	SELECT 		USERID,
				PASSWORD,
  				USERNAME,
				EMAIL,
				UPOINT,
				TO_CHAR(REGDATE, 'YYYY-MM-DD HH24:MI:SS') REGDATE
	FROM		tuser
	WHERE		userid = #{userid} 
  </select>
  
  <insert id="insertUser">
  	INSERT INTO TUSER 
  	VALUES (#{userid}, #{password}, #{username}, #{email}, 1000, SYSDATE);
  </insert>
  
  
  
  </mapper>